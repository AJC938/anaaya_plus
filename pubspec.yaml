name: anaaya_plus
description: "Anaaya Plus — mobile car service, on your schedule."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  go_router: ^17.5.0
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  cloud_firestore: ^6.8.0
  geolocator: ^14.0.3
  geocoding: ^5.0.0
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  firebase_messaging: ^16.6.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
